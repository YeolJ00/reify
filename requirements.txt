# conda env "warp", python 3.11 — versions verified working on 8x RTX A6000, CUDA 12.9
newton==1.4.0
warp-lang==1.15.0
numpy==2.4.6
matplotlib==3.11.1
pyyaml==6.0.3
opencv-python-headless==5.0.0.93   # LK tracking (src/track/lk.py)
trimesh==4.12.2                    # real-asset mesh loading (src/data/assets.py)
fast-simplification==0.1.13        # mesh decimation for rigid sim
# i2v models live in the separate "video" env (torch cu126 + diffusers 0.39);
# see docs/PROJECT_LOG.md. Not pinned here — different CUDA stack.

# i2v backends live in the `video` conda env (torch), not `warp`:
#   diffusers>=0.38  (includes SanaImageToVideoPipeline for the SANA-Video backend)
#   cotracker  (pip install git+https://github.com/facebookresearch/co-tracker.git)
#   Cosmos3-Nano backend runs in the isolated `cosmos` conda env instead:
#     torch cu128 + transformers 5.x + diffusers-from-main (Cosmos3OmniPipeline) + imageio
# rendering the rollout with newton.viewer.ViewerRTX (headless).
# ovrtx is the RTX backend; pyglet is its windowing dependency.
ovrtx==0.4.0.346409
pyglet==2.1.15
